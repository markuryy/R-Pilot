{
  "name": "r-pilot",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "setup": "node scripts/setup.js",
    "start": "bun run build && turbo run start"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "latest"
  },
  "packageManager": "bun@1.0.14",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "dependencies": {
    "@radix-ui/react-icons": "^1.3.1",
    "case-sensitive-paths-webpack-plugin": "^2.4.0",
    "install": "^0.13.0",
    "radix-ui": "^1.0.1"
  }
}
