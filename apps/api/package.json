{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd services && poetry run uvicorn main:app --reload",
    "start": "cd services && poetry run uvicorn main:app",
    "test": "cd services && poetry run pytest"
  }
}
